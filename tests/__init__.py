"""z-manip test suite — ROS2 contract tests (sim==real, attach-only).

Package init so tests import shared modules as ``from tests import helpers`` /
``contract``. Contains no test logic and no third-party imports.
"""
