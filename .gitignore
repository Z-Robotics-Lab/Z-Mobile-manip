# 密钥（.env.example 是样板；真值手工携带，绝不入库）
.env
configs/piper_home.json
configs/*_calibration.json
*.pem
*.key
*.license
*.bag
*.db3
*.mcap
*.pt
*.pth
*.onnx
*.engine
*.npy
*.npz
*.pcd
*.ply
# 例外：tests/data 下的录制夹具是测试的输入，体积小，必须入库
# （place_lab_crate_scene.npz 等原本靠 git add -f 进来，这里写明意图）
!tests/data/*.npz

# 只读参考仓（clone_deps.sh 拉取，不入库）
refs/

# 运行时产物
logs/
log/
var/
outputs/
artifacts/
.pytest_cache/
.coverage
htmlcov/
__pycache__/
.venv/
*.log

# Python 构建产物（pip install -e . 生成，不入库）
*.egg-info/
*.egg
build/
dist/
*.pyc
.claude/
docker/vamp/
tools/
flowdiagram.png
